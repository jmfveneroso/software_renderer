#version 330 core

in vec2 UV;

out vec3 color;

uniform sampler2D RenderedTextureSampler;
uniform sampler2D DepthSampler;

void main(){
  color = texture(RenderedTextureSampler, UV).xyz;
  // color = vec3((texture(DepthSampler, UV).x - 0.9) * 10);
}
